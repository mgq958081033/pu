

开发内容：

* 实现带线条的树形参考图。

* 做个简单交互：点击人员，弹出添加菜单，可横向添加配偶、
                竖向添加孩子节点。

* 可调用tree.json文件中包含基本数据字段。

* Html文件直接运行，不设置服务器，不用保存数据库。

* 时间宽裕则实现更多功能。


技术参考：

1、图中的节点块是用dom元素 + css，线条使用svg库绘制，不
   会svg，则用h5+c3实现；

2、vue框架，出于性能问题等考虑，尽量避免用canvas来实现。

